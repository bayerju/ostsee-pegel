# Working agreement

- Run `pnpm lint` and `pnpm typecheck` after every code change and before reporting the work as complete.
- Do not finish a task while either command exits with an error.
- Keep fixes scoped to the task and do not suppress lint rules merely to make a check pass.
